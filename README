
1. 代码编译
    cd rabbitLmU/
    mkdir build
    cd build/
    cmake ..
    make

    ./rabbit

2. add for own
    src/xxxSwU
    src/xxxSwU/src
    src/xxxSwU/inc
    src/xxxSwU/CMakeLists.txt

3. fwk


4. C++ 学习
    1. 数据结构
    2. 算法
    3. 模板
    4. 设计模式

    开源代码看看

5. 问题


6. 开源消息库
gRPC:
    gRPC是一种高性能的开源远程过程调用(RPC)框架,由Google开发并开源。它支持多种语言和平台,并使用Protocol Buffers作为默认的序列化和接口定义语言。
    gRPC提供了强大的功能,如双向流式传输、认证和授权等，适用于构建分布式系统和微服务架构。

ZeroMQ(ZMQ):
    ZeroMQ是一个快速的消息库,提供了分布式应用程序之间的异步消息传递功能。它支持多种消息传输模式，如请求/应答、发布/订阅、管道等，并且具有较低的延迟和良好的性能。

libevent:
    是一个高性能的事件通知库，用于开发高性能的网络服务器和客户端。它支持多种 I/O 多路复用技术，如 select、poll、epoll(在 Linux 上)、kqueue(在 BSD 系统上)以及
    Windows 的 IOCP 和 event ports。libevent 提供了轻量级、事件驱动的网络通信机制，使得开发者能够轻松地构建可扩展的服务器应用程序，同时避免了复杂的线程管理和锁的使用。
