
1. 代码编译
    cd rabbitLmU/
    mkdir build
    cd build/
    cmake ..
    make

    ./rabbit

2. add for own
    src/xxxSwU
    src/xxxSwU/src
    src/xxxSwU/inc
    src/xxxSwU/CMakeLists.txt

3. fwk


4. C++ 学习
    1. 数据结构
    2. 算法
    3. 模板
    4. 设计模式

    开源代码看看

5. 问题
    特化
    Substitution Failure Is Not An Error

6. 开源消息库
gRPC:
    gRPC是一种高性能的开源远程过程调用(RPC)框架,由Google开发并开源。它支持多种语言和平台,并使用Protocol Buffers作为默认的序列化和接口定义语言。
    gRPC提供了强大的功能,如双向流式传输、认证和授权等，适用于构建分布式系统和微服务架构。

Google Cloud Pub/Sub:
    Google开源的用于消息订阅和消息分发的库是Google Cloud Pub/Sub。它是一个可扩展的、全托管的消息代理服务,用于在分布式系统中进行消息传递和事件驱动的通信。
    Cloud Pub/Sub支持多种语言和平台,提供了可靠的异步消息传递，以及一些高级功能，如消息过滤、推送通知、订阅扩展等。
    https://cloud.google.com/pubsub/docs

ZeroMQ(ZMQ):
    ZeroMQ是一个快速的消息库,提供了分布式应用程序之间的异步消息传递功能。它支持多种消息传输模式，如请求/应答、发布/订阅、管道等，并且具有较低的延迟和良好的性能。

RabbitMQ:
    RabbitMQ是一个可靠的开源消息代理,实现了高级消息队列协议(AMQP)。它具有灵活的路由和消息分发机制，可以在分布式系统中进行可靠的消息传递，并提供可靠性、持久化和故障恢复的保证。